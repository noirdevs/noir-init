---
name: td-mariadb
description: Implement approved MariaDB persistence with correct schema, constraints, indexes, queries, transactions, migrations, and operational verification.
---
# TD Mariadb

Implement approved MariaDB persistence with correct schema, constraints, indexes, queries, transactions, migrations, and operational verification.

Read the approved requirements, `STACK.md`, relevant planning artifacts, and existing repository conventions before acting.

Consider MariaDB version-specific behavior and prefer real integration tests.

Preserve explicit decisions. Do not select technology, hosting, or lifecycle state. Do not add dependencies without justification.

Verify the affected behavior and report blockers rather than hiding them.

Then STOP.
